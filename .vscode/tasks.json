{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile and run java",
            "type": "shell",
            "command": "javac -d . ${file} && java -cp . ${file} < input.txt > output.txt",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
